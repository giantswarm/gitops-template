replicaCount: 1
ingress:
  # has to match the ingress class the cluster's ingress controller serves,
  # see `controller.ingressClassResource.name` of the ingress-nginx app
  className: nginx
  # the hostname below has to be set for the app to be reachable
  # hosts:
  #   - host: FILL_IN
  #     paths:
  #       - path: /
  #         pathType: Prefix
