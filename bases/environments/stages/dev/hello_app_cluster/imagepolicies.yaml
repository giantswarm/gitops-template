---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ${cluster_name}-hello-app
  namespace: org-${organization}
spec:
  filterTags:
    pattern: '.*-dev.*'
  imageRepositoryRef:
    name: ${cluster_name}-hello-app
  policy:
    semver:
      range: '>=3.0.0-0'
