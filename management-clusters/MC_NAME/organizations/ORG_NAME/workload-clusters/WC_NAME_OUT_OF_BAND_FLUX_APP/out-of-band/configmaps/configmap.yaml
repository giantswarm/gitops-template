apiVersion: v1
data:
  values: |-
    config_key_001: this is a deliberately long value number 001 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_002: this is a deliberately long value number 002 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_003: this is a deliberately long value number 003 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_004: this is a deliberately long value number 004 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_005: this is a deliberately long value number 005 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_006: this is a deliberately long value number 006 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_007: this is a deliberately long value number 007 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_008: this is a deliberately long value number 008 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_009: this is a deliberately long value number 009 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_010: this is a deliberately long value number 010 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_011: this is a deliberately long value number 011 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_012: this is a deliberately long value number 012 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_013: this is a deliberately long value number 013 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_014: this is a deliberately long value number 014 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_015: this is a deliberately long value number 015 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_016: this is a deliberately long value number 016 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_017: this is a deliberately long value number 017 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_018: this is a deliberately long value number 018 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_019: this is a deliberately long value number 019 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_020: this is a deliberately long value number 020 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_021: this is a deliberately long value number 021 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_022: this is a deliberately long value number 022 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_023: this is a deliberately long value number 023 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_024: this is a deliberately long value number 024 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_025: this is a deliberately long value number 025 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_026: this is a deliberately long value number 026 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_027: this is a deliberately long value number 027 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_028: this is a deliberately long value number 028 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_029: this is a deliberately long value number 029 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_030: this is a deliberately long value number 030 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_031: this is a deliberately long value number 031 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_032: this is a deliberately long value number 032 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_033: this is a deliberately long value number 033 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_034: this is a deliberately long value number 034 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_035: this is a deliberately long value number 035 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_036: this is a deliberately long value number 036 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_037: this is a deliberately long value number 037 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_038: this is a deliberately long value number 038 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_039: this is a deliberately long value number 039 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_040: this is a deliberately long value number 040 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_041: this is a deliberately long value number 041 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_042: this is a deliberately long value number 042 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_043: this is a deliberately long value number 043 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_044: this is a deliberately long value number 044 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_045: this is a deliberately long value number 045 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_046: this is a deliberately long value number 046 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_047: this is a deliberately long value number 047 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_048: this is a deliberately long value number 048 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_049: this is a deliberately long value number 049 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_050: this is a deliberately long value number 050 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_051: this is a deliberately long value number 051 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_052: this is a deliberately long value number 052 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_053: this is a deliberately long value number 053 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_054: this is a deliberately long value number 054 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_055: this is a deliberately long value number 055 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_056: this is a deliberately long value number 056 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_057: this is a deliberately long value number 057 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_058: this is a deliberately long value number 058 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_059: this is a deliberately long value number 059 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_060: this is a deliberately long value number 060 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_061: this is a deliberately long value number 061 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_062: this is a deliberately long value number 062 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_063: this is a deliberately long value number 063 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_064: this is a deliberately long value number 064 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_065: this is a deliberately long value number 065 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_066: this is a deliberately long value number 066 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_067: this is a deliberately long value number 067 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_068: this is a deliberately long value number 068 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_069: this is a deliberately long value number 069 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_070: this is a deliberately long value number 070 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_071: this is a deliberately long value number 071 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_072: this is a deliberately long value number 072 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_073: this is a deliberately long value number 073 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_074: this is a deliberately long value number 074 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_075: this is a deliberately long value number 075 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_076: this is a deliberately long value number 076 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_077: this is a deliberately long value number 077 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_078: this is a deliberately long value number 078 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_079: this is a deliberately long value number 079 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_080: this is a deliberately long value number 080 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_081: this is a deliberately long value number 081 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_082: this is a deliberately long value number 082 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_083: this is a deliberately long value number 083 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_084: this is a deliberately long value number 084 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_085: this is a deliberately long value number 085 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_086: this is a deliberately long value number 086 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_087: this is a deliberately long value number 087 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_088: this is a deliberately long value number 088 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_089: this is a deliberately long value number 089 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_090: this is a deliberately long value number 090 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_091: this is a deliberately long value number 091 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_092: this is a deliberately long value number 092 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_093: this is a deliberately long value number 093 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_094: this is a deliberately long value number 094 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_095: this is a deliberately long value number 095 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_096: this is a deliberately long value number 096 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_097: this is a deliberately long value number 097 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_098: this is a deliberately long value number 098 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_099: this is a deliberately long value number 099 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_100: this is a deliberately long value number 100 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_101: this is a deliberately long value number 101 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_102: this is a deliberately long value number 102 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_103: this is a deliberately long value number 103 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_104: this is a deliberately long value number 104 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_105: this is a deliberately long value number 105 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_106: this is a deliberately long value number 106 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_107: this is a deliberately long value number 107 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_108: this is a deliberately long value number 108 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_109: this is a deliberately long value number 109 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_110: this is a deliberately long value number 110 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_111: this is a deliberately long value number 111 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_112: this is a deliberately long value number 112 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_113: this is a deliberately long value number 113 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_114: this is a deliberately long value number 114 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_115: this is a deliberately long value number 115 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_116: this is a deliberately long value number 116 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_117: this is a deliberately long value number 117 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_118: this is a deliberately long value number 118 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_119: this is a deliberately long value number 119 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
    config_key_120: this is a deliberately long value number 120 used to make the dyff output exceed the per-file truncation cap of five thousand bytes
kind: ConfigMap
metadata:
  name: secret-to-be-created-directly-in-WC
  namespace: default
